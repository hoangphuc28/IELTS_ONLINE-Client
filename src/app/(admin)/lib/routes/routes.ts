const head = '/admin'
const dashBoard = `${head}/dashboard`
const exams = `${head}/exams`
const parts = `${head}/parts`
const accounts = `${head}/accounts`
const routes = {
    head,
    dashBoard,
    exams,
    parts,
    accounts,
}
export default routes